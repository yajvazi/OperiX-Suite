import { invoiceTotals, paymentState } from "./invoice-calculations";
import type { ClientRow, InvoiceDraft, InvoiceTemplateConfig } from "./models";

export interface HtmlCompany {
  name?: string; email?: string; phone?: string; address?: string; city?: string;
  country?: string; taxId?: string; bankName?: string; iban?: string; website?: string;
  signatureUrl?: string; stampUrl?: string;
}

const esc=(value:unknown)=>String(value??"").replace(/[&<>"']/g,character=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;", "'":"&#39;"}[character]!));
const number=(value:number)=>new Intl.NumberFormat("de-DE",{minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(value)||0);
const date=(value:string)=>{if(!value)return "";const parsed=new Date(value);return `${String(parsed.getDate()).padStart(2,"0")}/${String(parsed.getMonth()+1).padStart(2,"0")}/${parsed.getFullYear()}`;};

const defaultColumns={rowNumber:true,sku:false,description:true,quantity:true,unit:true,unitPrice:true,discount:true,taxRate:false,lineTotal:true,grossPrice:false};
const defaultLabels={invoice:"INVOICE",billTo:"Bill To",date:"Date",due:"Due Date",item:"Description",quantity:"Qty",price:"Unit Price",total:"Amount",subtotal:"Subtotal",tax:"Tax",discount:"Discount",totalDue:"Total Due",notes:"Notes",terms:"Terms",unit:"Unit",payment:"Payment",taxId:"Tax ID",email:"Email",phone:"Tel",address:"Address",authorizedSignature:"Authorized Signature",customerSignature:"Customer Signature"};

const corporateStyle=`
@page{size:A4;margin:0}*{margin:0;padding:0;box-sizing:border-box}html,body{width:210mm;margin:0 auto;font-family:Arial,Helvetica,sans-serif;font-size:14px;color:#000;background:#fff}body{height:297mm;max-height:297mm;padding:10mm;display:flex;flex-direction:column;overflow:hidden}.main-content{flex:1 0 auto}.top-bar{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:20px;border-bottom:2px solid #000;padding-bottom:15px}.brand-name{font-size:32px;font-weight:900;letter-spacing:1px;text-transform:uppercase;margin-bottom:20px}.invoice-header-line{font-size:18px;font-weight:900;letter-spacing:.5px}.qr-section{text-align:right}.info-section{display:flex;justify-content:space-between;margin-bottom:20px;padding-bottom:10px;border-bottom:1px solid #ccc}.info-title{font-weight:bold;font-size:15px;margin-bottom:5px;border-bottom:1px solid #ccc;padding-bottom:2px}.info-row{display:flex;margin-bottom:4px}.label{width:100px;font-weight:bold;font-size:13px}.value{flex:1;font-size:13px}.meta-grid{display:flex;gap:10px;margin-bottom:20px;font-size:13px}.meta-item{border:1px solid #ccc;padding:8px;flex:1}.meta-label{font-weight:bold;display:block;margin-bottom:2px}table{width:100%;border-collapse:collapse;margin-bottom:20px;font-size:12px}thead{display:table-header-group}tr{page-break-inside:avoid;break-inside:avoid}th{background:#333;color:#fff;padding:10px 6px;text-align:center;font-weight:bold;border:1px solid #000;font-size:11px}td{border:1px solid #000;padding:8px 5px}.totals-section{display:flex;justify-content:flex-end;margin-bottom:30px}.totals-table{width:300px;text-align:right;font-size:14px}.total-row{display:flex;justify-content:space-between;padding:4px 0}.total-final{font-weight:900;font-size:18px;border-top:2px solid #000;border-bottom:2px solid #000;padding:8px 0;margin-top:5px}.paid-stamp{color:#1769aa;border:2px solid #1769aa;border-radius:3px;display:inline-block;width:122px;height:48px;padding:6px 8px;transform:rotate(-7deg);font-weight:900;letter-spacing:1.5px;text-align:center;font-size:16px;margin-right:24px}.paid-stamp small{display:block;font-size:9px;letter-spacing:.3px;margin-top:2px}.signatures{display:flex;justify-content:space-between;margin-bottom:10px;text-align:center;font-size:14px}.sig-block{width:22%}.sig-line{border-bottom:1px solid #000;height:62px;margin-bottom:5px;display:flex;align-items:flex-end;justify-content:center}.seller-signature{max-height:58px;max-width:100%;object-fit:contain}.page-footer{display:flex;justify-content:space-between;border-top:1px solid #000;padding-top:10px;font-size:12px;margin-top:auto}.footer-col{flex:1}.muted{color:#555;font-size:11px}
`;

const receiptStyle=`@page{size:50mm auto;margin:0}*{box-sizing:border-box}html,body{margin:0;padding:0;width:50mm;background:#fff;color:#000}body{padding:3mm 3mm 5mm;font-family:"Courier New",Courier,monospace;font-size:10px;line-height:1.25}.center{text-align:center}.right{text-align:right}.bold{font-weight:700}.company{font-size:15px;font-weight:700;margin-bottom:2px}.small{font-size:9px}.meta{width:100%;margin:7px 0 4px;border-collapse:collapse}.meta td{padding:1px 0;vertical-align:top}.meta td:first-child{width:17mm;font-weight:700}.rule{border-top:1px dashed #000;margin:5px 0}.section-title{text-align:center;font-weight:700;margin:5px 0}.items{width:100%;border-collapse:collapse;table-layout:fixed}.items th{border-bottom:1px solid #000;padding:2px 1px;text-align:right;font-size:9px}.items th:first-child{text-align:left}.items td{padding:2px 1px;text-align:right;vertical-align:top;overflow-wrap:anywhere}.items td:first-child{text-align:left}.description{width:34%}.quantity{width:18%}.price,.value{width:24%}.totals{width:100%;border-collapse:collapse;margin-top:3px}.totals td{padding:2px 0}.totals td:last-child{text-align:right;font-weight:700}.grand-total td{border-top:1px solid #000;border-bottom:1px solid #000;font-size:13px;padding:4px 0}.payment{margin-top:6px}.footer{margin-top:9px;text-align:center;font-size:9px}`;

function corporateMarkup(draft:InvoiceDraft,client:ClientRow|undefined,company:HtmlCompany,config:InvoiceTemplateConfig){
  const totals=invoiceTotals(draft);const payment=paymentState(totals.total,draft.amount_received);const columns={...defaultColumns,...(config.visibleColumns||{})};const labels={...defaultLabels,...(config.labels||{})};
  const qrTarget=`${process.env.NEXT_PUBLIC_INVOICE_APP_URL||"https://invoice.operixsuite.com"}/qr/${encodeURIComponent(draft.invoice_number)}`;
  const qr=`https://api.qrserver.com/v1/create-qr-code/?size=100x100&data=${encodeURIComponent(qrTarget)}&color=000000`;
  const rows=draft.items.map((item,index)=>{const net=item.quantity*item.unit_price*(1-item.discount/100);const total=net*(1+item.tax_rate/100);return `<tr>${columns.rowNumber?`<td style="text-align:center">${index+1}</td>`:""}${columns.sku?`<td>${esc(item.sku||"-")}</td>`:""}${columns.description?`<td>${esc(item.description)}</td>`:""}${columns.quantity?`<td style="text-align:right">${item.quantity}</td>`:""}${columns.unit?`<td style="text-align:center">${esc(item.unit||"pcs")}</td>`:""}${columns.unitPrice?`<td style="text-align:right">${number(item.unit_price)}</td>`:""}${columns.discount?`<td style="text-align:right">${item.discount}%</td>`:""}${columns.taxRate?`<td style="text-align:right">${item.tax_rate}%</td>`:""}${columns.lineTotal?`<td style="text-align:right;font-weight:bold">${number(total)}</td>`:""}${columns.grossPrice?`<td style="text-align:right">${number(item.unit_price*(1+item.tax_rate/100))}</td>`:""}</tr>`;}).join("");
  const headers=`${columns.rowNumber?`<th>Nr</th>`:""}${columns.sku?`<th>Shifra</th>`:""}${columns.description?`<th style="text-align:left">${esc(labels.item)}</th>`:""}${columns.quantity?`<th>${esc(labels.quantity)}</th>`:""}${columns.unit?`<th>Njësia</th>`:""}${columns.unitPrice?`<th>${esc(labels.price)}</th>`:""}${columns.discount?`<th>${esc(labels.discount)}</th>`:""}${columns.taxRate?`<th>TVSH %</th>`:""}${columns.lineTotal?`<th>${esc(labels.total)}</th>`:""}${columns.grossPrice?`<th>Çmimi me TVSH</th>`:""}`;
  return `<style>${corporateStyle}</style><div class="main-content"><div class="top-bar"><div><div class="brand-name">${config.showLogo!==false?esc(company.name||"EMRI I BIZNESIT"):""}</div><div class="invoice-header-line">${esc(labels.invoice)}: ${esc(draft.invoice_number)}</div></div><div class="qr-section">${config.showQrCode!==false?`<img src="${qr}" style="height:80px;width:80px" alt="Invoice QR"/>`:""}</div></div><div class="info-section"><div style="width:100%"><div class="info-title">${esc(labels.billTo)}</div><div class="value" style="font-weight:bold;font-size:14px;margin-bottom:6px">${esc(client?.name||"-")}</div><div style="display:flex;gap:40px"><div style="flex:1"><div class="info-row"><span class="label">Tax ID:</span><span class="value">${esc(client?.tax_id||"-")}</span></div><div class="info-row"><span class="label">Email:</span><span class="value">${esc(client?.email||"-")}</span></div></div><div style="flex:1"><div class="info-row"><span class="label">Tel:</span><span class="value">${esc(client?.phone||"-")}</span></div><div class="info-row"><span class="label">Address:</span><span class="value">${esc(client?.address||"-")}</span></div></div></div></div></div><div class="meta-grid"><div class="meta-item"><span class="meta-label">${esc(labels.date)}:</span>${date(draft.issue_date)}</div><div class="meta-item"><span class="meta-label">${esc(labels.due)}:</span>${date(draft.due_date)}</div><div class="meta-item"><span class="meta-label">Payment:</span>${esc(draft.payment_method)}</div></div><table><thead><tr>${headers}</tr></thead><tbody>${rows}</tbody></table><div class="totals-section">${payment.paid&&config.showStamp!==false?`<div class="paid-stamp">E PAGUAR<small>${date(new Date().toISOString())}</small></div>`:""}<div class="totals-table"><div class="total-row"><span>${esc(labels.subtotal)}:</span><span>${number(totals.subtotal)}</span></div>${config.showDiscount!==false?`<div class="total-row"><span>${esc(labels.discount)}:</span><span>-${number(totals.discount)}</span></div>`:""}${config.showTax!==false?`<div class="total-row"><span>${esc(labels.tax)}:</span><span>${number(totals.tax)}</span></div>`:""}<div class="total-row total-final"><span>${esc(payment.paid?labels.totalDue:labels.total)}:</span><span>${number(payment.paid?0:totals.total)}</span></div></div></div>${config.showSignature!==false||config.showBuyerSignature!==false?`<div class="signatures">${config.showSignature!==false?`<div class="sig-block"><div class="sig-line">${company.signatureUrl?`<img class="seller-signature" src="${esc(company.signatureUrl)}" alt="Signature"/>`:""}</div>Authorized Signature</div>`:""}${config.showBuyerSignature!==false?`<div class="sig-block"><div class="sig-line"></div>Customer Signature</div>`:""}</div>`:""}<div class="page-footer"><div class="footer-col">${config.showBankDetails!==false?`<b>Bank Details:</b> ${esc(company.bankName)} | IBAN: ${esc(company.iban)}`:""}${config.showNotes!==false&&draft.notes?`<div class="muted">${esc(draft.notes)}</div>`:""}</div><div class="footer-col" style="text-align:right">${esc(company.email)}<br/>${esc(company.website)}<br/>${esc(company.taxId)}</div></div></div>`;
}

function receiptMarkup(draft:InvoiceDraft,client:ClientRow|undefined,company:HtmlCompany){const totals=invoiceTotals(draft);const payment=paymentState(totals.total,draft.amount_received);const rows=draft.items.map(item=>`<tr><td>${esc(item.description)}${item.sku?`<div class="small">${esc(item.sku)}</div>`:""}</td><td>${number(item.quantity)}</td><td>${number(item.unit_price)}</td><td>${number(item.quantity*item.unit_price*(1-item.discount/100)*(1+item.tax_rate/100))}</td></tr>`).join("");return `<style>${receiptStyle}</style><header class="center"><div class="company">${esc(company.name||"BIZNESI")}</div>${company.address?`<div class="small">${esc(company.address)}</div>`:""}${company.phone?`<div class="small">Tel: ${esc(company.phone)}</div>`:""}${company.taxId?`<div class="small">NUI/NR. FISKAL: ${esc(company.taxId)}</div>`:""}</header><table class="meta"><tr><td>Nr:</td><td>${esc(draft.invoice_number)}</td></tr><tr><td>Data:</td><td>${date(draft.issue_date)}</td></tr><tr><td>Klienti:</td><td>${esc(client?.name||"-")}</td></tr><tr><td>TVSH:</td><td>${number(totals.tax)} EUR</td></tr></table><div class="rule"></div><div class="section-title">&lt;&lt; PËRMBLEDHJE E FATURËS &gt;&gt;</div><table class="items"><thead><tr><th class="description">Përshkrim</th><th class="quantity">Sasi</th><th class="price">Çmim</th><th class="value">Vlera</th></tr></thead><tbody>${rows}</tbody></table><table class="totals"><tr><td>Nëntotali:</td><td>${number(totals.subtotal)} EUR</td></tr>${totals.discount>0?`<tr><td>Zbritja:</td><td>-${number(totals.discount)} EUR</td></tr>`:""}<tr><td>TVSH:</td><td>${number(totals.tax)} EUR</td></tr><tr class="grand-total"><td>TOTALI:</td><td>${number(totals.total)} EUR</td></tr></table>${draft.payment_method?`<div class="payment"><span class="bold">Pagesa:</span> ${esc(draft.payment_method)}</div>`:""}${Number(draft.amount_received)>0?`<table class="totals"><tr><td>Pranuar:</td><td>${number(draft.amount_received)} EUR</td></tr><tr><td>Kusuri:</td><td>${number(payment.change)} EUR</td></tr></table>`:""}<div class="footer"><div class="rule"></div><div class="bold">JU FALEMINDERIT!</div>${company.email?`<div>${esc(company.email)}</div>`:""}${company.website?`<div>${esc(company.website)}</div>`:""}</div>`;}

export function invoiceMarkup(draft:InvoiceDraft,client?:ClientRow,receipt=false,company:HtmlCompany={},config:InvoiceTemplateConfig={}){return receipt?receiptMarkup(draft,client,company):corporateMarkup(draft,client,company,config);}
export function invoiceHtml(draft:InvoiceDraft,client?:ClientRow,receipt=false,company:HtmlCompany={},config:InvoiceTemplateConfig={}){const overrides=receipt?"":"<style>html,body{font-size:16px!important}body{height:297mm!important;min-height:297mm!important;padding:7mm!important}.main-content{display:flex!important;flex-direction:column!important;height:283mm!important;min-height:0!important}.signatures{margin-top:auto!important;height:9em!important;flex-shrink:0!important;transform:translateY(8mm)}.page-footer{margin-top:0!important;height:20mm!important;flex-shrink:0!important}.page-footer .footer-col:last-child{text-align:center!important}.seller-signature{max-height:120px!important;width:280px!important;filter:brightness(0) saturate(100%) invert(25%) sepia(98%) saturate(2500%) hue-rotate(210deg) brightness(92%) contrast(108%)}</style>";return `<!doctype html><html><head><meta charset="utf-8">${overrides}</head><body>${invoiceMarkup(draft,client,receipt,company,config)}</body></html>`;}
